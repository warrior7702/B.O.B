{
  "name": "agent-bus",
  "version": "1.0.0",
  "description": "Local SQLite message bus for FBCA agents — zero npm dependencies",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {}
}
