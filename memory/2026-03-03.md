# Memory — March 3, 2026

## Morning Carry-Forward from March 2

### Config Audit Alert
- `~/.openclaw/openclaw.json` changed since last baseline (flagged at session end)
- Billy did not confirm making this change — needs investigation at start of session
- Do NOT touch the file — report to Billy only

### Active Blockers
1. **Door mapping config** — 9 AM reminder sent. Billy needs to define which doors unlock for which PCO event types + time buffers. This unblocks the PCO → Door Control bridge build.
2. **Domain decision** — Wednesday 9 AM reminder set. tools.fbca.org vs fbcatools.org. Discuss with Andy.
3. **Cornerstone Kimi credits** — their problem to fix, not B.O.B.'s
4. **Agent comms revamp** — both messaging systems need work (2 tasks on board)

### Today's Priority Order
1. Investigate config audit alert (report to Billy, don't fix)
2. Door mapping config (9 AM reminder fires)
3. Continue ACE Phase 2 after mapping is defined
4. Domain decision prep for Wednesday

### Project Status

**ACE Phase 1:** ✅ COMPLETE
- Dashboard, memory, crons, COMPASS all live
- 10 cron jobs running

**ACE Phase 2:** 🟡 IN PROGRESS
- PCO connected and proven
- Door Control API mapped
- Bridge blocked on door mapping
- Teams bot research queued
- Agent comms revamp queued
- Mobile-friendly Mission Control in progress

**Church Ops Integrations:**
- PCO attendance: live, Sunday 1 PM auto-report
- Door Control: mapped, bridge building pending
- Email read access: queued
- Vendor invoice lookup: queued
- OneDrive path guardrails: queued

### Key Facts to Remember
- Convex deployment: **merry-rabbit-524.convex.cloud** (NOT merry-mandrill-290)
- Agent bridge also on merry-rabbit-524
- Tasks still query merry-mandrill-290 (original tasks table lives there)
- Mission Control running on localhost:3000, accessible via Tailscale 100.99.198.88:3000
- Ollama: dead, removed from LaunchAgents, never restart
- Model: anthropic/claude-sonnet-4-6 — never suggest Ollama as alternative
- PCO attendance: always filter attended==true, paginate all pages, add visitors_count separately
- FBCA Door Control API: https://100.123.239.124:5003 — open on local network, no auth needed yet
- Hardware (HID Signo/Mercury firmware) = last phase, don't start yet

### Lessons Learned Yesterday
- SSH monitor was detecting itself (predicate in log args triggered macOS logging) — fixed
- Convex `grep -c .` returns exit 1 on zero results — always add `|| true`
- PCO attendance: `total_count` from meta = roster size, not attendance. Must filter attended==true
- `npx convex deploy` deploys to different URL than `npx convex dev` — check .env.local matches
- agentComms index bug: `toAgent` index required both fields, workaround is full table scan with filter

### COMPASS
- Fires 8 AM daily (same time as daily brief)
- Generates 3 FBCA-specific ideas → stores in Convex → Telegram ping
- Review queue in Mission Control under COMPASS tab

### Cornerstone Division of Ownership
- B.O.B.: PCO queries, attendance, infrastructure, hardware layer (later)
- Cornerstone: Door Control software, mobile credential UI, Azure AD, orchestration
- Shared: PCO → Door Control bridge, testing

## 8 AM COMPASS Run — Complete
- Convex `ideas` table was not deployed — ran `npx convex deploy` to fix
- 3 ideas stored to merry-rabbit-524, Telegram sent (msgId 1841)
- ⚠️ COMPASS cron has wrong Convex URL hardcoded: `merry-mandrill-290` → should be `merry-rabbit-524`
- Ideas: Door Mapping Wizard (low/high), Email Extractor (medium/high), Bulletin Auto-Draft (medium/medium)

## 8 AM Daily Brief — Sent
- Delivered to Telegram (msgId 1835)
- Key items: SSH brute force last night (6-11 attempts ~9:40 PM), openclaw.json flag at 5:39 AM, door mapping still blocking ACE Phase 2
- Convex merry-mandrill-290 HTTP endpoint returned 404 — tasks sourced from memory carry-forward
- Disk 2% — clean

---

## March 3 Morning Session (9 AM – 10:38 AM)

### ACE Phase 2 Continued — Proactive Agents

#### CALVIN 📅 — Tomorrow's Events (LIVE)
- Cron job ID: `c3c15892-5cc0-49be-96e4-c8a7a29c4978`
- Fires daily at 5 PM CST
- Pulls all PCO event instances for tomorrow, per-instance (not per-event — critical fix)
- **Root cause of room duplication solved**: querying resource_bookings by event returns ALL recurring instances' bookings; must query by event_instance ID for clean single-day data
- Filters: skip HVAC events, dedupe rooms with dict.fromkeys()
- Detects flags: Building Access, AV, Room Setup
- Output: clean checklist per event, Telegram message
- Tested against real Wednesday March 4 data — 24 events, clean output

#### DOORY 🐕 — Door Offline Alerts (LIVE)
- Cron job ID: `6cbe0c05-af3c-49fb-9c51-ff770ac0fc14`
- Runs every 15 minutes
- Checks /api/health — alerts if any component goes unhealthy
- Checks /api/doors — alerts if any mapped door isActive flips to false
- **Silent when clean** — Telegram only on real failures
- **Stale sync threshold**: NOT used — all 12 doors synced 12.6h ago simultaneously (normal batch sync pattern), threshold would cause false positives
- Mission Control DOORY tab: live door grid, system health panel, 60-second auto-refresh
- Fixed runtime error: `health.checks ?? {}` guard needed (checks can be null)

### New Agent Domains Added to Master Plan
| Agent | Domain |
|-------|--------|
| MIST 📡 | Juniper Mist — APs, switches, firewall |
| AKITA 🗺️ | AkitaBox — building resources, space maps |
| GATES 🪟 | Microsoft 365 & Azure (Billy named this one) |

- GATES named after Bill Gates — M365, Azure AD, Intune, Teams, Exchange, SharePoint
- Billy context: running entire FBCA IT operation solo — doors, cameras, network, servers, phones, HVAC, M365, Azure, PCO, WordPress, door control, streaming, user support. These agents exist to give him leverage.

### Key Decisions Today
- **PCO → Door bridge**: tabled until Cornerstone finishes minimum model; no cron job, just a script when ready
- **WordPress automation**: skip for now, not Billy's lane
- **SECURITY agent**: skip for now — SSH not enabled on this Mac, nothing to protect against
- **DOORY stale threshold**: don't use sync time as alert trigger — batch sync is normal, use isActive and health endpoint only
- **ACE sequence**: staying in order per master plan (not skipping around)

### Next Up
- Weeks 9-10: Human-Agent Handoff (approval workflows, task delegation, failure escalation)
- OR: MIST — Juniper network health monitoring (real infrastructure to watch)
- SECURITY agent deferred until SSH is enabled or real threat surface exists

### Active Cron Jobs (full list)
| ID | Name | Schedule |
|----|------|----------|
| `06edd102` | Daily Brief | 8 AM CST daily |
| `3c034eb2` | SSH Monitor | Every 10 min |
| `aeb59832` | Disk Monitor | Hourly |
| `c5931d13` | Config Audit | 6 AM CST daily |
| `e9ca3f39` | Memory Log | 8 PM CST daily |
| `dac6c5cf` | Learning Extractor | Sunday 6 PM CST |
| `b3d383bf` | COMPASS | 8 AM CST daily |
| `bf9821ce` | Sunday Attendance | Sunday 1 PM CST |
| `91968cd8` | Domain Reminder | Wednesday 9 AM (one-shot) |
| `c3c15892` | CALVIN | 5 PM CST daily |
| `6cbe0c05` | DOORY | Every 15 min |
