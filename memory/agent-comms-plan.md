# Agent Communication Plan — Phase 1

## Scope
- Two OpenClaw instances (both on MacBooks in Texas)
- Goal: Help each other, learn, coordinate restores

## Data to Sync
- Identity hints: ✓ enabled by default (display_name, pronouns, timezone)
- Memory cues: ✗ disabled by default
- Config snapshots: ✓ enabled
- Restore requests: Manual approval required

## Auth
- Token-based over HTTPS
- Per-session opt-in
- Audit logging enabled

## Deliverables Location
- ~/workspace/Deliverable-* (all 5 created)
